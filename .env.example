# Authentication credentials
APP_USERNAME=morgane
APP_PASSWORD=changeme_with_secure_password

# JWT Secret (generate a new one for production)
JWT_SECRET=generate_random_secret_here

# Database path
DB_PATH=tasks.db
